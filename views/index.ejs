<%- include("./partials/header.ejs") %>

  <% if (locals.bandname) { %>
    <h1>
      <%=bandname%>
    </h1>

    <% } else { %>
      <h1>Click to generate Bandname!🤟</h1>

      <% } %>


        <form action="/submitbandname" method="POST">
          <input type="submit" value="Generate Band Name">
        </form>


        <% if (locals.sillynames) { %>
          <h1>
            <%=sillynames%>
          </h1>

          <% } else { %>
            <h1>Click to generate Sillynames!🤪</h1>

            <% } %>



              <form action="/submitsillyname" method="POST">
                <input type="submit" value="Generate Silly Name">
              </form>


              <% if (locals.superheronames) { %>
                <h1>
                  <%=superheronames%>
                </h1>

                <% } else { %>
                  <h1>Click to generate SuperHero names!🤪</h1>

                  <% } %>

                    <form action="/submitsuperhero" method="POST">
                      <input type="submit" value="Generate superhero Name">
                    </form>
                    <%- include("./partials/footer.ejs") %>